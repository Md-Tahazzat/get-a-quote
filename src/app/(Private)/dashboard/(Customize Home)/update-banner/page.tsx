import { setMetaData } from "@/utility/updateTitle/updateTitle";

export const metadata = setMetaData("Update Banner of Home");

const UpdateBannerPage = () => {
  return (
    <div>
      <h1>From update banner page</h1>
    </div>
  );
};

export default UpdateBannerPage;
